const fs = require('fs');

// fs.writeFileSync('notes.txt', 'File was created by me')

// fs.appendFileSync('notes.txt', ' data to append');

// fs.copyFile('notes.txt','destination.txt', (err)=> {
//     if(err) throw err
// })

// fs.renameSync('newFile.txt', 'newFile2.txt');

// const files = fs.readdirSync('./');
// files.forEach(file => console.log(file));

// const words = fs.mkdirSync('notes');